import { Ratio } from "react-bootstrap"
import UseTitle from '../../components/UseTitle'

const Home = () => {
  UseTitle('Home');

  return (

<section>hello</section>
    
  )
}

export default Home